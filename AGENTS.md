# 项目开发铁律

> 本文件是 VS Code Copilot 自动加载的项目规则，适用于本工作区（`e:\小说`）及其下所有项目。
> 主要项目：`novel_game`（小说互动游戏引擎）。

## 1. 技术栈（已确定，不得擅自更换）

- 开工前必须明确技术选型，一旦确定，后续开发不得擅自更换。
- 本工作区现有选型：Python 3.10+ / FastAPI + Uvicorn / ChromaDB / sentence-transformers（bge-base-zh-v1.5）/ DeepSeek（OpenAI 兼容）/ LangGraph / 原生 HTML+JS+CSS / D3.js。
- 新项目开工前先确认选型，确认后写入文档再动代码。

## 2. 目录结构与职责

- 新项目开工前，必须先输出完整目录结构并说明各目录职责；确认后方可编码。
- 目录遵循分层架构（如 api / agents / memory / pipeline / models / config），严禁跨层调用。
- 已有项目：新增文件必须落在既有分层内，不得随手新建目录。

## 3. 代码设计原则（最高优先级）

- **高内聚低耦合，单一职责。**
- **严禁过度设计**：不要为了“绝对完美”写出难以理解的复杂抽象代码。
- **严禁兜底与猜测**：不知道接口字段或业务逻辑时，必须停下来提问，不许瞎编；不许写冗余的备选方案和兜底逻辑。
- **性能底线**：不考虑极端小众情况，避免后端计算冗长，避免前端响应变慢。
- **简洁可读**：代码优先保证可读性和简洁性。

## 4. 工作流（每项任务强制遵循）

1. 先出计划（涉及哪些文件/方法/大致逻辑），等确认。
2. 确认后执行，生成代码。
3. 完成后自我 Review，检查是否符合分层和设计原则。
4. 有不确定的地方立刻提问，不要猜测。

## 5. 窗口协作规范（多 Agent 场景）

- 每个新窗口/新会话必须先读取本规则。
- 规划会话只做决策，不写代码；执行会话按任务单编码，完成后贴 Review 结果。

## 6. 本工作区事实（避免重复踩坑）

- 一律用中文回复，代码注释也用中文。
- 启动后端：`cd e:\小说\novel_game` 后 `python -m api.main`（或 `uvicorn api.main:app --port 8000`）；改代码后需重启进程。
- CLI 试玩：`python play.py`（编排与网页端共用 `agents/graph.py` 同一张图）。
- **Pylance 报 `Unable to import 'config' / 'fastapi'` 是假阳性**：工作区根是 `e:\小说`，而模块按 `novel_game` 为根导入。不要为此改动 import 写法。
- `chroma_db/`、`data/sessions/`、`data/bookshelf.json`、`data/character_cache/` 是运行数据，不得手工编辑或随意删除（删除会导致存档/人物缓存丢失）。
- 改 `.env` 中的密钥时不要打印密钥内容。
